# Финальный чек-лист

- [ ] `git status --short` не показывает незакоммиченных файлов сдачи.
- [ ] `docker compose up -d --build` завершается без ошибок.
- [ ] `local_eval.py` возвращает `PASS`.
- [ ] `local_eval.py --runs 10` даёт 10/10 положительных прогонов.
- [ ] `make_submission.py` воспроизводит закоммиченный `submission.csv`.
- [ ] Контрактные smoke-тесты проходят.
- [ ] Dashboard открывается на `http://localhost:5173`.
- [ ] Все пять вкладок dashboard проверены вручную.
- [ ] Кнопка Run local simulation возвращает результат.
- [ ] README читается на GitHub и команды запуска корректны.
- [ ] В презентации результаты названы mock-симуляцией.
- [ ] `agent.py` и `submission.csv` находятся в папке пакета участника.
- [ ] Последний обязательный почасовой коммит отправлен в `main`.
