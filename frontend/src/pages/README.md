# Dashboard pages

The five views are intentionally kept in the dashboard shell while the API contract is stabilised. They use the typed service layer and can be extracted into route components without changing data access.
